language: php
php:
  - 7.0

sudo: false

env:
  global:
    - SIMPLETEST_DB=sqlite://tmp/site.sqlite
    - SIMPLETEST_BASE_URL="http://127.0.0.1:8080"
  matrix:

before_install:
  - echo 'sendmail_path = /bin/true' >> ~/.phpenv/versions/$(phpenv version-name)/etc/conf.d/travis.ini
  - phpenv config-rm xdebug.ini

install:
  - composer --verbose install

before_script:
  - cd $TRAVIS_BUILD_DIR/web
  - ./../vendor/bin/drush site-install --verbose --yes --db-url=sqlite://tmp/site.sqlite
  - ./../vendor/bin/drush config-set "system.site" uuid "2748a6a9-0c67-43c6-aa47-71df4737a69f" --yes
  - ./../vendor/bin/drush ev '\Drupal::entityManager()->getStorage("shortcut_set")->load("default")->delete();'
  # modules needs to be enabled before and after configuration import, as import disables new modules
  - for module in `cat $TRAVIS_BUILD_DIR/common.modules`; do ./../vendor/bin/drush pm-enable $module --yes; done
  - ./../vendor/bin/drush config-import --yes
  - for module in `cat $TRAVIS_BUILD_DIR/common.modules`; do ./../vendor/bin/drush pm-enable $module --yes; done
  - ./../vendor/bin/drush runserver http://127.0.0.1:8080 &
  - sleep 3
script:
  - cd $TRAVIS_BUILD_DIR
  - vendor/bin/behat  
notifications:
  email: false
